
  # Register JSON in global.css

  This is a code bundle for Register JSON in global.css. The original project is available at https://www.figma.com/design/ZBpBa2LbWFylugj35LLwe8/Register-JSON-in-global.css.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  